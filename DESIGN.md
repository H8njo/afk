# Design: AFK - AI Focus Keeper

Generated by /office-hours on 2026-06-10
Branch: unknown
Repo: afk
Status: APPROVED
Mode: Builder → Product

## Problem Statement

AI 코딩 도구(Claude Code, Cursor 등)에 작업을 시키고 기다리는 동안, 개발자들은 수동으로 Cmd+Tab으로 앱을 전환하고, "끝났나?" 계속 확인하며, 핸드폰을 보거나 멍하니 기다린다. 이 컨텍스트 스위칭 비용이 쌓여서 생산성을 죽인다.

## What Makes This Cool

AI 작업 상태를 감지해서 자동으로 앱을 전환하는 도구는 세상에 없다. "AI에 요청 보내면 유튜브가 뜨고, AI가 끝나면 터미널이 돌아온다" — 이 자동 전환 자체가 "whoa" 포인트. 단순하지만 아무도 만들지 않은 것.

## Demand Evidence

- 본인이 매일 겪는 불편함 (수동 전환 + 반복 확인)
- 주변 개발자들에게서 비슷한 불편을 들음
- "The Paradox of Waiting for AI to Code" 등 문제를 인정하는 콘텐츠가 이미 존재
- 해결하는 도구는 아직 없음 (시장 갭)

## Target User & Narrowest Wedge

**타겟:** macOS에서 Claude Code를 쓰는 개발자
**Wedge:** Claude Code hook 시스템을 활용한 자동 앱 전환 (가장 기술적으로 깔끔한 진입점)

## Constraints

- macOS only (초기)
- Claude Code hooks 의존 (초기, 이후 Cursor/VSCode 확장)
- Accessibility 권한 필요 (macOS 앱 제어)
- 배터리/리소스 최소 사용 (백그라운드 상주 앱)

## Premises

1. AI 코딩 도구의 작업 상태를 외부에서 감지 가능 (Claude Code hooks로 입증)
2. macOS에서 자동 앱 전환이 UX를 해치지 않음 (progressive interruption으로 해결)
3. 최초 타겟은 macOS + Claude Code 사용자로 충분
4. 무료로 시작 → 대시보드/프로 기능으로 수익화

## Cross-Model Perspective

독립 AI 리뷰에서 나온 핵심 인사이트:

- **Progressive Interruption:** 바로 전환하지 말고, 메뉴바 카운트다운 → 보더 글로우 → 전환 순서로 점진적 알림. 전제 #2 (전환이 짜증날 수 있음) 해결의 핵심.
- **Task-aware break routing (v2):** 10초 작업이면 전환 안 하고, 2분이면 유튜브 띄우기. 작업 시간에 따라 break 수준을 조절. (작업 시간 예측 로직 필요, v1에서는 일률적 딜레이로 시작)
- **진짜 흥분 포인트:** 크로스 디바이스 (iPhone에서 Mac AI 상태 확인)는 "기계를 베이비시팅하지 않아도 되는 자유"가 핵심 가치.
- **Hammerspoon:** 50% 솔루션이 될 수 있지만, 제품화를 생각하면 네이티브 Swift 앱이 맞음.

## Approaches Considered

### Approach A: Shell + Hammerspoon (Minimal Viable)
- Stack: Shell script + Lua
- Effort: S (1-2일)
- 장점: 가장 빠른 프로토타입, Hammerspoon의 macOS 시스템 접근
- 단점: Hammerspoon 설치 필요 (배포 허들), 설정 UI 없음, 제품화 어려움

### Approach B: Native Swift Menu Bar App (Ideal Architecture) - CHOSEN
- Stack: Swift + SwiftUI
- Effort: M (1-2주)
- 장점: 독립 실행, SwiftUI 설정 UI, Mac App Store 배포 가능, 전문적인 제품 느낌
- 단점: Swift 학습 필요할 수 있음, 개발 시간 더 걸림

### Approach C: Electron + Node.js (Cross-platform)
- Stack: Electron + TypeScript + React
- Effort: M (1-2주)
- 장점: 웹 기술 활용, 크로스 플랫폼 확장, 대시보드 UI 쉬움
- 단점: 메모리 100MB+ (메뉴바 앱치고 무거움), 네이티브 느낌 부족

## Recommended Approach

**Approach B: Native Swift Menu Bar App**

Swift + SwiftUI로 macOS 네이티브 메뉴바 앱. 독립 실행 가능하고, Mac App Store 배포를 통한 제품화가 자연스럽다. 개발자 도구로서의 신뢰감도 네이티브 앱이 가장 높다.

## Architecture

```
┌─────────────────────────────────────────────────┐
│                    AFK App                       │
│              (Swift Menu Bar App)                │
│                                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │
│  │ State    │  │ App      │  │ Settings     │   │
│  │ Monitor  │  │ Switcher │  │ (SwiftUI)    │   │
│  │          │  │          │  │              │   │
│  │ - File   │  │ - Focus  │  │ - Break app  │   │
│  │   watcher│  │ - Hide   │  │ - Delay      │   │
│  │ - Hook   │  │ - Restore│  │ - AI tools   │   │
│  │   events │  │          │  │ - Dashboard  │   │
│  └──────────┘  └──────────┘  └──────────────┘   │
│       ↑                                           │
│  ┌──────────┐                                     │
│  │ Claude   │  ~/.afk/sessions/{id}.json            │
│  │ Code     │  ← Hook writes state here           │
│  │ Hook     │                                     │
│  └──────────┘                                     │
└─────────────────────────────────────────────────┘
```

### Core Components

**1. AI State Monitor**
- `FSEvents` / `DispatchSource`로 `~/.afk/sessions/` 디렉토리 감시
- Claude Code hook이 세션별 상태 파일 기록 → 파일 변경 감지 → 이벤트 발생
- 여러 세션 중 하나라도 `waiting_for_user`면 즉시 AI 앱으로 전환
- 상태: `idle` | `working` | `waiting_for_user`
- 앱이 실행 중이지 않으면 자동 실행 후 포커스 (fallback)

**2. Claude Code Hook (설치 시 자동 등록)**

Hook은 별도 스크립트(`afk-hook.sh`)를 호출하여 shell 변수 확장 문제를 방지한다.

```bash
# Scripts/afk-hook.sh
#!/bin/bash
# Usage: afk-hook.sh <state> [session_id]
STATE="$1"
SESSION_ID="${2:-default}"
mkdir -p ~/.afk/sessions
echo "{\"state\":\"$STATE\",\"session_id\":\"$SESSION_ID\",\"ts\":\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"}" > ~/.afk/sessions/"$SESSION_ID".json
```

```json
// ~/.claude/hooks.json
{
  "hooks": {
    "UserPromptSubmit": [
      { "command": "~/.afk/bin/afk-hook.sh working $CLAUDE_SESSION_ID" }
    ],
    "Notification": [
      { "command": "~/.afk/bin/afk-hook.sh waiting_for_user $CLAUDE_SESSION_ID" }
    ],
    "Stop": [
      { "command": "~/.afk/bin/afk-hook.sh idle $CLAUDE_SESSION_ID" }
    ]
  }
}
```

**State file schema:**
```json
{
  "state": "working | waiting_for_user | idle",
  "session_id": "string (unique per Claude Code session)",
  "ts": "ISO 8601 timestamp"
}
```

파일 위치: `~/.afk/sessions/{session_id}.json` (멀티 세션 지원, race condition 방지)

**3. App Switcher**
- `NSWorkspace.shared.open` / `NSRunningApplication.activate`로 앱 포커스
- Progressive interruption 옵션:
  - 딜레이 2초 후 전환 (기본, thrash 방지)
  - 즉시 전환
  - 점진적 (메뉴바 알림 → 전환)
- 앱 미실행 시: `NSWorkspace.shared.open`으로 자동 실행 후 포커스

**4. Menu Bar UI**
- 상태 아이콘: 녹색 점 (AI 작업 중), 빨간 점 (사용자 필요), 회색 (idle)
- 클릭 → 드롭다운: 현재 상태, 일시정지, 설정 열기
- 설정 창 (SwiftUI): break 앱 선택, 딜레이 설정, AI 도구 관리

**5. Dashboard (v2)**
- AI 작업 시간, 딴짓 시간 통계
- 일별/주별 패턴
- SwiftUI Charts

## State Machine

```
         user_prompt_submit
IDLE ──────────────────────→ WORKING
 ↑                              │
 │  stop                        │ notification / stop
 │                              ↓
 └────────────────────── WAITING_FOR_USER
          stop
```

**용어 정의:**
- **AI 앱 (Source App):** AI가 돌아가는 앱 (예: Terminal, iTerm2, Cursor, VSCode)
- **Break 앱 (Break App):** AI 작업 중 사용자가 전환될 앱 (예: YouTube in Safari, 게임 등)

**전환 규칙:**
- `IDLE → WORKING`: break 앱 포커스 (설정된 딜레이 후, 기본 2초)
- `WORKING → WAITING_FOR_USER`: AI 앱 포커스 (즉시)
- `WORKING → IDLE`: AI 앱 포커스 (즉시)
- `WAITING_FOR_USER → WORKING`: break 앱 포커스
- `WAITING_FOR_USER → IDLE`: AI 앱 포커스 (세션 종료/타임아웃)

## File Structure

```
AFK/
├── AFK.xcodeproj
├── AFK/
│   ├── AFKApp.swift              # App entry point (MenuBarExtra)
│   ├── Models/
│   │   ├── AIState.swift         # State enum + model
│   │   └── AppConfig.swift       # User settings model
│   ├── Services/
│   │   ├── StateMonitor.swift    # File watcher for state.json
│   │   ├── AppSwitcher.swift     # App focus/hide logic
│   │   └── HookInstaller.swift   # Claude Code hook setup
│   ├── Views/
│   │   ├── MenuBarView.swift     # Status icon + dropdown
│   │   ├── SettingsView.swift    # Settings window
│   │   └── DashboardView.swift   # Usage stats (v2)
│   └── Resources/
│       └── Assets.xcassets
├── Scripts/
│   └── afk-hook.sh              # Claude Code hook script
└── README.md
```

## Open Questions

1. **Hook 충돌:** 사용자가 이미 Claude Code hooks를 쓰고 있으면? → 기존 hooks에 추가하는 방식으로 처리
2. **멀티 세션:** Claude Code 세션이 여러 개일 때? → 해결: 세션별 파일 (`~/.afk/sessions/{id}.json`)로 분리, 디렉토리 감시
3. **Cursor/VSCode 지원:** Extension API로 상태 감지? 별도 플러그인 필요?
4. **Mac App Store 심사:** Accessibility 권한 + 다른 앱 제어 → 심사 통과 가능한지 확인 필요
5. **Progressive interruption 구현:** 작업 시간 예측 없이 어떻게 break 수준을 결정할지

## Success Criteria

- [ ] Claude Code에 요청 보내면 3초 내 break 앱 포커스
- [ ] AI 작업 완료/질문 시 1초 내 AI 앱 복귀
- [ ] 메뉴바에서 현재 상태 확인 가능
- [ ] 일시정지/재개 가능
- [ ] 첫 설치 후 5분 내 동작 가능
- [ ] 배터리 영향 < 1%

## Distribution Plan

- **Phase 1:** GitHub 릴리스 (.dmg 다운로드)
- **Phase 2:** Homebrew cask (`brew install --cask afk`)
- **Phase 3:** Mac App Store (수익화와 함께, 단 Accessibility 권한 + 앱 제어로 인해 심사 거부 가능성 있음. 대안: 직접 배포 + 공증(notarization)으로 장기 유지)
- **CI/CD:** GitHub Actions로 빌드 → 자동 릴리스

## Revenue Model (Future)

- **Free:** 기본 앱 전환 + 메뉴바 상태
- **Pro ($4.99/mo or $29.99 one-time):**
  - Dashboard + 통계
  - 멀티 AI 도구 지원
  - 커스텀 전환 룰
  - 크로스 디바이스 (iPhone companion app)

## Next Steps

1. Xcode 프로젝트 생성 + 메뉴바 앱 스켈레톤
2. Claude Code hook script 작성
3. FileSystemObjectSource로 state.json 감시
4. NSRunningApplication으로 앱 전환 구현
5. SwiftUI 설정 창 (break 앱 선택)
6. 테스트 + 화면 녹화
7. GitHub에 공개

## What I noticed about how you think

- "심플하게 하지만 풀 대시보드도 있으면 좋을거같은데" — 핵심과 확장의 균형을 자연스럽게 잡는다. 코어 먼저, 대시보드 나중. 이건 좋은 제품 감각.
- "크로스 디바이스" — 10x 비전을 물었을 때 기능 나열이 아니라 사용 맥락을 말했다. "컴퓨터 앞에서 걸어나가도 괜찮은 자유." 이건 기능이 아니라 가치.
- "다른 사용자들에게도 풀거야 잘되면 뭐 수익이 날수도있고" — 자연스럽게 사이드 프로젝트에서 제품으로 전환. 만들면서 방향을 잡는 타입.
- "잠재적 이슈 알아서 해결하고 진행해" — 결정 피로를 최소화하고 실행에 집중하는 스타일. 빌더 마인드셋.
